Place optional Syzygy WDL tablebase files (`*.rtbw`) in this directory to bundle them with the Apple Swift package resource bundle.

The engine currently probes WDL during search. DTZ files (`*.rtbz`) are not needed by the current Apple wrapper.
