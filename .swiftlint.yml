disabled_rules:
  - force_cast
  - force_try
  - identifier_name
  - file_length
  - leading_whitespace
  - nesting

excluded:
  - .build

line_length:
  error: 125

trailing_whitespace:
  severity: error
